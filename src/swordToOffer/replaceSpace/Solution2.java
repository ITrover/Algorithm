package swordToOffer.replaceSpace;

/**
 * 双指针
 * 先统计字符串中空格的个数k
 * 移动后的数组大小 = 原大小 + 2*k
 * 一个指针指向数组的最后，另外一个指向新数组的最后
 * 从后面开始挪动
 */
class Solution2 {
    public String replaceSpace(String s) {
        //正则表达式
        return null;
    }
}
